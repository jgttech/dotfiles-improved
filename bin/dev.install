#!/usr/bin/env bash
set -euo pipefail

# [WARNING - DEVELOPMENT ONLY]
# Intended to be run within an container for testing.

ctx=".dotfiles/bin/install"

# Default test.
cat $ctx | bash

# Go test.
# cat $ctx | DOTFILES_DIR=`pwd` DOTFILES_LANG="go" bash

# Zig test.
# cat $ctx | DOTFILES_DIR=`pwd` DOTFILES_LANG="zig" bash

# Odin test.
# cat $ctx | DOTFILES_DIR=`pwd` DOTFILES_LANG="odin" bash

# TypeScript test.
# cat $ctx | DOTFILES_DIR=`pwd` DOTFILES_LANG="typescript" bash

# Python test.
# cat $ctx | DOTFILES_DIR=`pwd` DOTFILES_LANG="python" bash
